'use strict'

/**
 * @param {string} _bundleId
 * @returns {'shell' | 'filesystem' | 'system_settings' | null}
 */
function getSentinelCategory(_bundleId) {
  return null
}

module.exports = { getSentinelCategory }
